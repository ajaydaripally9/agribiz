# Set the API root URL when frontend and PHP backend are deployed separately.
# Example: VITE_API_BASE_URL=https://myshop-api.onrender.com
# Leave blank when the frontend is served from the same origin as the PHP backend.
# In local development, keep this blank so Vite proxies PHP requests to http://localhost:8000.
VITE_API_BASE_URL=
